#include <stdio.h>
int main()
{
    int a, b;
    scanf("%d%d", &a, &b);
    // Addition
    printf("A+B=%d", a + b);
    // Substraction
    printf("A-B=%d", a - b);
    // Multiplication
    printf("A*B=%d", a * b);
    // Division
    printf("A/B=%f", a / b);

    return 0;
}